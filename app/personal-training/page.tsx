import { Section } from "../section";

export default function PersonalTrainingPage() {
  return <Section title="Personal Training" text="A deeper individual workflow: client profile → assessment → goals → program → session → feedback → progress → adaptation." />;
}
