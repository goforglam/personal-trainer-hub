import { Section } from "../section";
export default function ProgressPage() {
  return <Section title="Progress" text="Track outcomes, adherence, measurements and meaningful changes over time." />;
}
