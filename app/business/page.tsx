import { Section } from "../section";

export default function BusinessPage() {
  return <Section title="Business" text="Your trainer business at a glance: schedule, hours, group fitness delivery, personal training workload and — later — revenue and KPIs." />;
}
